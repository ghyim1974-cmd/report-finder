# DESIGN.md — Report Finder (PDCA Design)

> 출처: [PLAN.md](PLAN.md) / [PRD.md](PRD.md) 기반 / 방법론: bkit PDCA - Design 단계

## 1. 화면 구성 (무엇이 어디에 보이는지)

PC 웹 브라우저 기준 **한 화면(단일 페이지)**. 톤은 심플·미니멀, 메인 색상은 차분한 네이비/블루.

```
┌─────────────────────────────────────────────────────────────┐
│  [상단 바]  📄 Report Finder    보고서 3개 ▾    │  ← 앱 이름 + 보고서 목록 버튼
├───────────────────────┬─────────────────────────────────────┤
│ [왼쪽] 보고서 패널     │ [오른쪽] 질문·답변 패널              │
│                       │                                     │
│  ┌ 파일 올리기 ─────┐ │  ┌ 대화 영역 ──────────────────┐   │
│  │ 드래그 또는 선택 │ │  │ Q: 3분기 매출은?            │   │
│  │ (PDF·Word·txt)   │ │  │ A: 3분기 매출은 12억원…     │   │
│  └──────────────────┘ │  │    📎 출처: 3분기보고.pdf/p2 │   │
│                       │  └─────────────────────────────┘   │
│  저장된 보고서 목록   │                                     │
│  • 3분기보고.pdf  🗑  │  ┌ 질문 입력창 ────────────┐        │
│  • 인사규정.docx  🗑  │  │ 질문을 입력하세요…  [전송]│        │
│  • 회의록.txt     🗑  │  └──────────────────────────┘        │
└───────────────────────┴─────────────────────────────────────┘
```

- **상단 바**: 앱 이름 + 현재 저장 현황(문서 수 / 총 용량) — 50개·200MB 한도를 눈으로 확인
- **왼쪽 패널**: ①파일 올리기 영역(형식·용량 안내), ②저장된 보고서 목록 + 각 항목에 요약(📝)·삭제(🗑) 버튼
- **오른쪽 패널**: ①질문·답변이 쌓이는 대화 영역(답변 끝에 📎출처 표시), ②하단 질문 입력창 + 전송 버튼
- **상태 표시**: 업로드 중·답변 생성 중·오류는 각 패널에 짧은 안내 문구로 표시

## 2. 데이터 흐름 (입력 → 처리 → 출력)

**흐름 A — 보고서 업로드·저장**
```
입력: 파일 선택(PDF·Word·txt)
  → [검증] 형식·용량(20MB)·저장 한도(50개·200MB) 확인  ─(위반)→ 거절 안내
  → [텍스트 추출] 파일에서 글자 뽑기  ─(글자 없음)→ "텍스트를 읽을 수 없습니다"
  → [조각내기] 문단 단위로 잘게 나눔
  → [임베딩] 각 조각을 숫자 벡터로 변환(OpenAI)
출력: Supabase에 조각+벡터 저장 → 왼쪽 목록에 새 보고서 표시
```

**흐름 B — 질문·답변(RAG)**
```
입력: 사용자가 질문 입력
  → [질문 임베딩] 질문을 숫자 벡터로 변환(OpenAI)
  → [검색] Supabase에서 의미가 가까운 보고서 조각 찾기  ─(문서 없음)→ "먼저 보고서를 업로드해 주세요"
  → [답변 생성] 찾은 조각만 근거로 OpenAI가 답변 작성  ─(근거 없음)→ "보고서에서 찾을 수 없습니다"
  → [출처 붙이기] 보고서명/페이지·문단 위치 표시 (상충 시 여러 출처 모두)
출력: 오른쪽 대화 영역에 답변 + 📎출처 표시
```

**흐름 C — 보고서 요약**
```
입력: 목록에서 특정 보고서의 "요약(📝)" 버튼 클릭
  → [조각 모으기] 해당 보고서의 chunks를 순서대로 불러옴
  → [요약 생성] 그 내용만 근거로 OpenAI가 핵심 3~5문장 요약 (한국어, 추측 금지)
출력: 오른쪽 대화 영역에 요약 + 📎출처(보고서명) 표시
```

## 3. 확정 기술 스택 (옵션 B — 보통)

> 사용자 선택: **옵션 B (보통·추천)**, 2026-07-23. 난이도별 3옵션 중 균형이 가장 좋아 채택.

| 구성 요소 | 선택 | 쉬운 설명 (한 줄) |
|-----------|------|-------------------|
| 화면(프론트) | 순수 HTML / JavaScript | 배운 그대로, 별도 틀 없이 웹페이지로 만듭니다. |
| 서버(백엔드) | Node.js (`server.js`), 라우트 약간 분리 | 파일 하나로 시작하되 업로드·질문 처리 부분을 나눠 정리합니다. |
| 텍스트 추출 | `pdf-parse`(PDF), `mammoth`(Word), 기본 읽기(txt) | 파일에서 글자를 뽑아주는 검증된 도구를 끼워 깨진 파일도 안정적으로 처리합니다. |
| 문서·벡터 저장 | Supabase (`pgvector`) | 보고서 조각과 그 의미(벡터)를 저장하고 비슷한 내용을 빨리 찾습니다. |
| AI | OpenAI (`text-embedding-3-small` + GPT) | 글을 숫자로 바꾸는 임베딩과 답변 생성을 키 하나로 처리합니다. |
| 실행·배포 | 로컬 우선 (필요 시 Vercel + 접근 비밀번호) | 평소엔 내 PC에서, 배포하면 비밀번호로 나만 접속합니다. |

**채택 이유:** 옵션 A의 유일한 약점인 텍스트 추출을 검증된 라이브러리(`pdf-parse`/`mammoth`)로 보강해, PRD의 "PDF·Word·txt 처리 / 스캔본 거절" 규칙을 안정적으로 지킬 수 있음. 옵션 C(Next.js)는 이번 목표(개인용·핵심 기능)에는 과함.

## 4. 데이터 모델 (Supabase 저장 구조)

출처(페이지/문단 위치) 표시를 보장하려면 조각마다 위치 정보를 함께 저장해야 한다. 두 개의 표를 둔다.

**`documents` — 보고서 1건**
| 칸 | 타입 | 설명 |
|---|---|---|
| `id` | uuid (PK) | 보고서 고유 번호 |
| `name` | text | 파일명 (출처 표기에 사용) |
| `file_type` | text | `pdf` / `word` / `txt` |
| `size_bytes` | int8 | 파일 용량 (한도 계산용) |
| `uploaded_at` | timestamptz | 업로드 시각 |

**`chunks` — 보고서를 문단 단위로 나눈 조각**
| 칸 | 타입 | 설명 |
|---|---|---|
| `id` | uuid (PK) | 조각 고유 번호 |
| `document_id` | uuid (FK→documents) | 소속 보고서 |
| `chunk_index` | int | 문단 순번 (문서 내 순서) |
| `content` | text | 조각 원문 텍스트 |
| `page` | int (nullable) | PDF의 페이지 번호 (Word·txt는 `null`) |
| `location_label` | text | 출처 표기 문자열 (예: `p.2` 또는 `문단 5`) |
| `embedding` | vector(1536) | OpenAI `text-embedding-3-small` 임베딩 |

- 저장 한도(50개·200MB)는 `documents`의 개수·`size_bytes` 합계로 검사한다.
- 유사도 검색은 `chunks.embedding`에 대한 `pgvector` 코사인 거리로 수행한다.

## 5. API 목록 (서버 주소)

| 메서드 | 경로 | 하는 일 | 성공 응답 |
|---|---|---|---|
| POST | `/api/upload` | 파일 검증→텍스트 추출→청킹→임베딩→저장 | `{ document }` |
| GET | `/api/documents` | 저장된 보고서 목록 + 저장 현황(개수/용량) | `{ documents, usage }` |
| DELETE | `/api/documents/:id` | 보고서 1건 삭제(조각 포함) | `{ ok: true }` |
| POST | `/api/ask` | 질문 임베딩→유사 조각 검색→답변 생성→출처 | `{ answer, sources }` |
| POST | `/api/documents/:id/summary` | 해당 보고서 조각 전체→요약 생성→출처 | `{ summary, source }` |

- 오류는 `{ error: { message } }` 형태로 통일하고, 이유(형식·용량·한도·텍스트 없음 등)를 담는다.

## 6. 출처 확보 방식 (형식별)

| 형식 | 위치 추출 방법 | 출처 표기 |
|---|---|---|
| PDF | `pdf-parse`의 페이지별 렌더 콜백(또는 `pdfjs-dist`)으로 **페이지 단위로** 텍스트를 모아 조각에 `page` 저장 | `보고서명 / p.N` |
| Word(.docx) | `mammoth`로 문단 추출 — **페이지 개념 없음** → 문단 순번 사용 | `보고서명 / 문단 N` |
| txt | 빈 줄 기준 문단 분리 → 문단 순번 사용 | `보고서명 / 문단 N` |

> **PRD 조정 사항:** PRD 5장은 "PDF·Word는 페이지"라고 했으나, .docx는 안정적인 페이지 번호를 낼 수 없어 **Word는 txt와 동일하게 "문단 위치"로 출처를 표기**한다. (출처 100% 표시 목표는 그대로 유지)

## 7. 답변 규칙과 예외 처리

**답변 생성 규칙 (PRD 5장):**
- 업로드된 보고서 내용에만 근거. 근거 없으면 `"보고서에서 찾을 수 없습니다"` (추측 금지)
- **한국어, 3~5문장 이내**로 간결하게
- 답변 끝에 출처 100% 표시. 여러 보고서가 상충하면 각 출처와 함께 모두 제시.

**요약 생성 규칙 (PRD 5장 3번):**
- 해당 보고서 내용에만 근거 (추측·외부 지식 금지)
- 한국어, 핵심 3~5문장(또는 불릿 3~5개)
- 요약 끝에 출처(보고서명) 표시. 사용자가 요청할 때만 생성(자동 실행 없음).

**예외 상황별 안내 문구 (PRD 9장):**
| 상황 | 안내 |
|---|---|
| 미지원 형식·용량/한도 초과 | "지원하지 않는 형식이거나 용량을 초과했습니다" (저장 안 함) |
| 텍스트 없는 문서(스캔·사진 PDF) | "문서에서 텍스트를 읽을 수 없습니다" (저장 안 함) |
| 보고서에 답 없음 | "보고서에서 찾을 수 없습니다" |
| 저장 문서 없이 질문 | "먼저 보고서를 업로드해 주세요" |
| AI 응답 실패·네트워크 오류 | "일시적인 오류가 발생했습니다. 잠시 후 다시 시도해 주세요" |
| 긴 문서 처리 지연 | 처리 중 상태 표시(스피너/문구) 후 완료 시 갱신 |

## 8. 테스트 계획 (성공 기준 검증 — 수동)

무거운 자동화 없이, PRD 3장 성공 기준을 개인이 직접 확인하는 방식으로 검증한다.
- **정답률 90%↑**: 미리 정답을 아는 질문 20개로 답변을 직접 채점(정답/오답).
- **출처 100%**: 모든 답변에 출처가 붙는지 육안 확인.
- **규칙 준수**: (a) `.xlsx`·25MB·한도 초과 업로드 거절, (b) 스캔 PDF 거절, (c) 문서 없이 질문 시 안내 문구, (d) 근거 없는 질문에 "찾을 수 없습니다" 응답 확인.

## 9. 보류 항목

- **접근 비밀번호(W2):** 로컬 실행이 기본이므로 지금은 만들지 않는다. **Vercel 배포 시점(PLAN 12번, 선택)** 에 환경변수 기반 접근 비밀번호 입력 화면·검사 흐름을 추가한다.

## 10. 다음 단계

기술 스택·데이터 모델·API가 확정됐으므로, PDCA 흐름상 **Do(구현)** 로 넘어가 [PLAN.md](PLAN.md)의 1번 작업(서버 + 화면 뼈대)부터 착수한다.
각 작업은 CLAUDE.md의 검증 루프(변경→확인→리뷰→수정→요약)를 거친다.
