# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

사내 보고서(PDF·Word·txt)를 업로드하면 그 내용에 근거해 질문에 답하는 **개인용 RAG 챗봇**. 본인 혼자 쓰는 로컬 우선 앱이며, 로그인·회원가입은 만들지 않는다. 상세 요구사항은 [PRD.md](PRD.md)(전체), [prd_lite.md](prd_lite.md)(요약)에 있다.

**현재 상태: 기획 단계 — 코드·`package.json`·빌드/테스트 스크립트가 아직 없다.** 아래 "계획된 구조"는 PRD 8·10장을 근거로 한 예정 사항이며, 실제 구현 시 이 문서의 명령어·구조 항목을 갱신할 것.

## 반드시 지켜야 할 도메인 규칙

이 규칙들은 앱의 존재 이유이자 채점 기준이다. 구현할 때 절대 임의로 완화하지 말 것.

**업로드 & 저장**
- PDF·Word·txt **3가지 형식만** 허용, 그 외는 거절
- 파일 1개당 **최대 20MB**
- 저장 한도: **문서 50개 / 총 200MB**
- 텍스트가 추출되는 문서만 처리 — 글자 없는 스캔·사진 PDF는 거절 (OCR은 비범위)
- 텍스트는 **문단 단위 조각(chunk)** 으로 나눠 임베딩과 함께 저장
- 사용자가 목록에서 직접 삭제하기 전까지 보관하며, 삭제 기능 제공

**질문 답변 (RAG)**
- 답변은 **업로드된 보고서 내용에만 근거**한다. 근거가 없으면 반드시 `"보고서에서 찾을 수 없습니다"` — 추측·창작 금지
- 답변은 **한국어, 3~5문장 이내**로 간결하게
- 답변 끝에 **출처 100% 표시**: PDF·Word는 `보고서명 / 페이지`, txt는 `보고서명 / 문단 위치`
- 여러 보고서에 **상충되는 내용**이 있으면 하나만 고르지 말고 각각의 출처와 함께 **모두** 제시

**예외 상황**은 [PRD.md](PRD.md) 9장 참조 (형식·용량 초과, 텍스트 추출 불가, 저장 문서 없이 질문, 응답 실패·네트워크 오류 등 각각의 안내 문구가 정해져 있음).

## 계획된 아키텍처 (PRD 8장)

- **프론트엔드**: HTML / JavaScript — PC 웹 브라우저 전용 단일 화면 (모바일 대응 없음). 톤은 심플·미니멀, 메인 색상은 차분한 네이비/블루
- **백엔드**: Node.js (`server.js`)
- **벡터·문서 저장**: Supabase (임베딩 저장 및 유사도 검색)
- **AI**: **OpenAI API** — 임베딩(`text-embedding-3-small`)과 답변 생성(GPT)을 키 하나로 담당
- **배포**: 기본은 로컬 실행. Vercel 배포 시 **접근 비밀번호(환경변수)** 로 본인만 접속하게 함 — 다계정 로그인과는 다름

데이터 흐름: `업로드 → 텍스트 추출 → 문단 청킹 → 임베딩 후 Supabase 저장 → 질문 임베딩으로 유사 청크 검색 → 검색된 근거로 답변 생성(+출처)`

구현 순서는 [PRD.md](PRD.md) 10장의 12단계 개발 단위를 따른다.

## AI 제공자

**OpenAI API로 확정** (사용자 결정, 2026-07-23). 임베딩은 `text-embedding-3-small`, 답변 생성은 GPT를 사용하며 API 키 하나로 통합한다. PRD 8·10장 모두 OpenAI로 정리되어 있다.

## 보안·비밀 관리

- 대외비일 수 있는 사내 문서를 다룬다 — **외부 공유·업로드 금지**
- API 키와 접근 비밀번호는 소스에 하드코딩하지 말고 **`.env`(환경변수)** 로 관리, `.gitignore`에 등록해 저장소에 노출되지 않게 할 것

## 작업 절차 (검증 루프)

코드를 작성·수정할 때는 아래 루프를 **매번 반복**한다. 통과할 때까지 다음 단계로 넘어가지 않는다.

1. **변경한다** — 필요한 코드를 수정한다.
2. **결과를 직접 확인한다** — 브라우저로 열거나 실행해서 실제 동작을 눈으로 확인한다.
3. **스스로 코드 리뷰한다** — 방금 바꾼 코드를 직접 점검한다.
4. **문제가 있으면 고치고 다시 1)로** 돌아간다.
5. **통과하면** 무엇을 왜 바꿨는지 **한 줄로 요약**한다.

## 작업 규칙 (Claude가 지킬 것)

- **언어**: 모든 설명과 코드 주석은 **한국어**로 작성한다.
- **파일 위치**: 새 파일은 반드시 `my-app` 폴더 안에만 만든다.
- **변경 보고**: 코드를 바꾸면 무엇을 왜 바꿨는지 **한 줄로** 알린다.
- **비밀 파일**: `.env` 등 비밀 정보 파일은 `.gitignore`에 등록해 두고 **절대 커밋하지 않는다**.
- **삭제 절차**: 파일을 지워야 할 때는 바로 삭제하지 말고 `trash-can` 폴더를 만들어 그 안으로 **옮겨만** 둔다. 최종 삭제는 사용자가 직접 확인 후 진행한다.
- **서브에이전트 활용**: 이미 설치된 서브에이전트(bkit의 `gap-detector` 등)를 필요할 때마다 적극 활용한다.
- **서버 재시작 후 검증**: `server.js`·`routes/`·`lib/` 등 서버 코드를 고쳤으면, 검증 전에 **미리보기/개발 서버를 반드시 재시작**한다 (안 하면 옛 코드로 확인해 오판한다).
- **사용자 데이터 보호**: 검증용 시험 데이터는 **내가 만든 것만** 정리하고, 사용자가 올린 실제 보고서(DB·업로드 문서)는 절대 지우지 않는다. 구분이 애매하면 지우기 전에 확인한다.
