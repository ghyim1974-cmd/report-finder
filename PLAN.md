# PLAN.md — Report Finder (PDCA Plan)

> 출처: [PRD.md](PRD.md) 기반 / 방법론: bkit PDCA - Plan 단계

## 이번 사이클 목표

PRD에 정의된 기능 **전체**(보고서 업로드·저장 → RAG 질문답변 → 목록·삭제 → 보고서 요약 → 예외 처리)를 동작하는 **로컬 실행 앱**으로 완성한다. 기술 스택은 HTML/JS + Node.js(`server.js`) + Supabase + **OpenAI API**.

## 성공 기준 (측정 가능)

- **[정확성]** 미리 정답을 아는 테스트 질문 20개 중 보고서에 근거한 정답 비율 **90% 이상**
- **[출처]** 모든 답변에 출처(보고서명/페이지 또는 문단 위치) 표시 **100%**
- **[속도]** 특정 내용을 찾는 시간 기존 약 10분 → **30초 이내**
- **[규칙 준수]** 형식(PDF·Word·txt만)·용량(파일 20MB)·저장 한도(50개·200MB)·"근거 없으면 찾을 수 없습니다" 규칙이 모두 강제됨
- **[요약]** 보고서별 요약이 해당 보고서 내용에만 근거해 한국어 3~5문장으로 생성되고 출처(보고서명)가 표시됨

## 작업 순서 (먼저 되면 나머지가 쉬워지는 것부터)

1. Node.js 서버(`server.js`) + 기본 HTML 화면을 띄워 로컬 실행 뼈대를 만든다.
2. `.env`에 OpenAI·Supabase 키를 넣고 `.gitignore`에 등록한다. (이후 모든 단계의 전제)
3. Supabase에 보고서·조각(청크)·임베딩을 담을 테이블/벡터 저장소를 준비한다.
4. 파일 업로드 API/화면을 만들고 형식·용량·저장 한도 검증을 붙인다. (거절 규칙의 관문)
5. 업로드된 문서에서 텍스트를 추출한다 (글자 없는 스캔·사진 PDF는 거절).
6. 추출 텍스트를 문단 단위로 쪼개 임베딩과 함께 Supabase에 저장한다. (검색의 재료 완성)
7. 저장된 보고서 목록 보기·삭제 기능을 만든다.
8. 질문을 임베딩해 관련 보고서 조각을 검색하는 로직을 만든다. (RAG 검색부)
9. 검색된 근거로 OpenAI가 답변을 생성한다 (보고서에만 근거, 없으면 "찾을 수 없습니다").
10. 답변에 출처(보고서명/페이지·문단)를 붙이고, 상충 내용은 모두 표시한다.
11. 예외 상황을 처리한다 (문서 없이 질문, 응답 실패·네트워크 오류, 처리 지연 안내).
12. 보고서 요약 기능을 만든다 (목록의 "요약" 버튼 → 해당 보고서 요약 생성·표시 + 출처). (9번 답변 생성 코드 재사용)
13. (선택) 접근 비밀번호를 적용해 Vercel에 배포한다.

## 다음 단계

이 Plan을 확정하면 PDCA 흐름상 **Design → Do(구현) → Check(gap 분석) → Act(개선)** 로 진행한다.
작업 1번부터 착수하며, 각 작업은 CLAUDE.md의 검증 루프(변경→확인→리뷰→수정→요약)를 거친다.
