# Report Finder 서비스 기획서 (PRD)

- 작성자: ghyim
- 작성일: 2026-07-22

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

사내 보고서를 자주 다루는 개인 사용자가, 필요한 정보를 찾기 위해 매번 긴 보고서를 처음부터 읽어야 하는 불편을 겪고 있다. 특히 예전에 작성·수신한 보고서의 특정 내용을 다시 찾기가 어렵다. 이를 해결하기 위해, 사내 보고서를 업로드하면 그 내용을 바탕으로 답변해주는 개인용 챗봇 앱이 필요하다.

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

- 현재는 보고서 파일을 폴더에 저장해두고, 필요할 때마다 파일을 하나씩 열어 직접 찾아 읽는다.
- 한계
  - 어느 파일에 원하는 내용이 있는지 기억에 의존해야 한다.
  - 파일 이름·본문을 일일이 열어 확인해야 해서 시간이 오래 걸린다.
  - 긴 보고서에서 특정 수치·문장을 찾으려면 전체를 훑어봐야 한다.

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 특정 내용을 찾는 데 걸리는 시간을 기존 평균 약 10분(직접 파일을 열어 찾는 경우의 추정치) → **30초 이내**로 단축한다.
- 질문에 대한 답변 정답률(보고서에 근거한 정확한 답변 비율) **90% 이상**을 목표로 한다.
  - 측정 방법: 본인이 미리 정답을 아는 **테스트 질문 20개**를 만들어 답변을 직접 채점(정답/오답)해 비율을 계산한다.
- 답변에 **출처(보고서명·페이지 또는 위치)가 표시되는 비율 100%** 를 유지해, 사용자가 원문을 바로 확인할 수 있게 한다.

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

- 사용자: 사내 보고서를 다루는 본인(개인 사용자)
- 이용 흐름:
  보고서 파일 업로드 → 앱이 내용을 읽어 조각(문단 단위)으로 나눠 검색 가능한 형태로 저장 → 사용자가 궁금한 내용을 질문 → 앱이 저장된 보고서에서 근거를 찾아 답변(+출처 표시) → 사용자가 출처로 원문 확인

## 5. 주요 기능 (must / nice로 구분)

### [must] 1) 보고서 업로드 & 저장
- 설명: 사내 보고서 파일을 올리면, 앱이 내용을 읽어 문단 단위 조각으로 나눠 검색 가능한 형태로 저장한다. (원문 텍스트를 조각으로 나눠 저장)
- AI가 꼭 지켜야 할 규칙:
  - PDF·Word·txt 3가지 형식만 지원한다 (그 외 형식은 업로드를 거절)
  - 파일 1개당 최대 20MB까지만 허용한다
  - 저장 한도: 문서 최대 50개 / 총 200MB까지 저장한다
  - 텍스트가 들어 있는 문서만 처리한다 (글자가 없는 스캔·사진 PDF는 처리 대상에서 제외)
  - 저장된 보고서는 사용자가 직접 삭제하기 전까지 보관하며, 삭제(목록에서 제거) 기능을 제공한다

### [must] 2) 보고서 기반 질문 답변 (RAG)
- 설명: 저장된 보고서 내용에서 근거를 찾아 질문에 답한다.
- AI가 꼭 지켜야 할 규칙:
  - 답변은 반드시 업로드된 보고서 내용에만 근거하고, 자료에 없으면 "보고서에서 찾을 수 없습니다"라고 답한다 (추측 금지)
  - 답변은 한국어로, 핵심을 담은 문장형으로 3~5문장 이내로 간결하게 한다
  - 답변 끝에 출처를 함께 표시한다 (PDF는 "보고서명 / 페이지", 페이지 개념이 없는 Word·txt는 "보고서명 / 문단 위치"로 표기)
  - 여러 보고서에 서로 다른(상충되는) 내용이 있으면 하나만 고르지 말고 각각의 출처와 함께 모두 보여준다

### [must] 3) 보고서 자동 요약·리포트 생성
- 설명: 저장된 보고서를 골라 "요약" 버튼을 누르면, AI가 그 보고서의 핵심을 요약해 보여준다.
- AI가 꼭 지켜야 할 규칙:
  - 요약은 해당 보고서 내용에만 근거한다 (추측·외부 지식 금지)
  - 한국어로, 핵심 3~5문장(또는 불릿 3~5개)으로 간결하게 요약한다
  - 요약 끝에 출처(보고서명)를 표시한다
  - 요약은 사용자가 요청할 때 생성한다 (업로드만으로 자동 실행하지 않음)

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

- 범위 (이번에 만들 것)
  - 보고서 업로드 & 저장 (PDF·Word·txt, 파일당 20MB 이하 / 총 50개·200MB)
  - 보고서 기반 질문 답변 (RAG, 출처 표시)
  - 저장된 보고서 목록 보기 및 삭제
  - 보고서 자동 요약·리포트 생성
- 비범위 (이번에 만들지 않을 것)
  - 로그인·회원가입 / 여러 사용자 계정 (본인만 쓰므로 불필요)
  - 팀 공유·협업 기능 (댓글, 공유 링크 등)
  - 모바일 앱 (이번엔 하나의 화면 형태로만)
  - 스캔·사진 PDF의 글자 인식(OCR), 표·이미지 안의 내용 분석 (이번엔 텍스트 기반만)

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 사용 문서의 공개등급: 사내 보고서로, 대외비일 수 있으므로 **외부 공유 금지**. 본인만 사용한다.
- 접근 제한: 개인용이므로 로컬(본인 PC)에서 실행하는 것을 기본으로 한다. 만약 Vercel 등에 배포할 경우, 인터넷에 그대로 공개되지 않도록 **간단한 접근 비밀번호(환경변수로 관리)** 를 두어 본인만 접속하게 한다. (여러 계정을 만드는 로그인 기능과는 다름)
- 개인정보 여부: 보고서에 이름·연락처 등 개인정보가 포함될 수 있으므로, 불필요한 개인정보는 업로드하지 않도록 주의한다.
- API 키 관리: API 키는 소스코드에 직접 넣지 않고 **환경변수(.env)** 로 관리하며, .gitignore에 등록해 외부(깃 저장소 등)에 노출되지 않도록 한다.

## 8. 기술 스택
*개발에 사용할 도구 모음 (난이도별 옵션 중 내가 고른 것과 그 이유)*

**선택: 옵션 2 (보통) — 지금까지 실습한 것과 같은 구성**

- 구성 도구
  - 화면(프론트엔드): HTML / JavaScript
  - 서버(백엔드): Node.js (server.js)
  - AI: OpenAI API — 임베딩(text-embedding-3-small)과 답변 생성(GPT)을 모두 담당 (키 하나로 통합)
  - 문서·벡터 저장: Supabase
  - 배포: Vercel (기본은 로컬 실행, 배포 시 접근 비밀번호 적용)
- 고른 이유
  - 바로 옆 hello-page 실습에서 이미 써본 것과 같은 구성이라 가장 익숙하다.
  - "PDF·Word·txt만 허용 / 파일당 20MB 제한 / 답변에 출처 표시" 같은 우리만의 규칙을 직접 넣어 구현할 수 있다.
  - 완성형 서비스(옵션 1)보다 자유롭고, 전문 프레임워크(옵션 3)보다 학습 부담이 적어 실습에 적합하다.

## 9. 예외 상황 처리
*사용자가 겪을 수 있는 예외 상황과 앱의 대응*

- 지원하지 않는 형식·용량 초과 업로드 (예: .xlsx, 25MB 파일, 총 한도 초과): 저장하지 않고 "지원하지 않는 형식이거나 용량을 초과했습니다"라고 이유를 명확히 안내한다.
- 글자가 없는 보고서 (스캔·사진 PDF 등 텍스트 추출 불가): "문서에서 텍스트를 읽을 수 없습니다"라고 안내하고 저장하지 않는다.
- 보고서에 답이 없는 질문: "보고서에서 찾을 수 없습니다"라고 답하며, 반복해 물어도 추측으로 지어내지 않는다.
- 여러 보고서에 상충되는 내용: 하나만 고르지 않고, 각 보고서의 내용과 출처를 함께 제시한다.
- 저장된 보고서가 없는 상태에서 질문: "먼저 보고서를 업로드해 주세요"라고 안내한다.
- (기타) AI 응답 실패·네트워크 오류, 아주 긴 보고서 처리 지연 시: 오류/대기 상태를 사용자에게 알린다.

## 10. 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눠, 만들 순서대로 정렬*

1. 프로젝트 뼈대 만들기 (Node.js 서버 + 기본 HTML 화면 띄우기)
2. 환경변수(.env) 설정 (OpenAI API 키, Supabase 키) + .gitignore 등록
3. Supabase에 보고서 저장용 테이블·벡터 저장소 준비
4. 파일 업로드 화면 만들기 + 형식(PDF·Word·txt)·용량(20MB)·저장 한도(50개·200MB) 검증
5. 업로드된 문서에서 텍스트 추출 (글자 없는 문서는 거절)
6. 추출한 텍스트를 문단 단위 조각으로 나눠 Supabase에 저장(임베딩 포함)
7. 저장된 보고서 목록 보기·삭제 기능 만들기
8. 질문 입력 화면 만들기 + 질문으로 관련 보고서 조각 검색
9. 검색된 근거로 OpenAI(GPT)가 답변 생성 (보고서에만 근거, 없으면 "찾을 수 없습니다")
10. 답변에 출처(보고서명/페이지·문단) 표시 + 상충 내용은 모두 표시
11. 예외 상황 처리 붙이기 (문서 없이 질문, 응답 실패·네트워크 오류 등)
12. 보고서 요약 기능 만들기 (목록에서 "요약" 버튼 → 해당 보고서 요약 생성·표시 + 출처)
13. (선택) 접근 비밀번호 적용 후 Vercel 배포
